# Hoisted layout. Originally required by Metro, which could not follow
# pnpm's symlinked store; kept because the lockfile and Docker build are
# built around it. Switching to pnpm's default isolated linker is safe but
# needs a clean reinstall, so it is a deliberate change, not a drive-by.
node-linker=hoisted
link-workspace-packages=true
